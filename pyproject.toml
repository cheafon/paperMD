[project]
name = "papermd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llama-index-llms-openai>=0.6.5",
    "python-dotenv>=1.1.1",
]
